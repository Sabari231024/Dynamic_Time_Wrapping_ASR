#ifndef DISTANCE_H
#define DISTANCE_H

// This is the function prototype (or declaration).
// It tells the compiler that a function named "distance" exists,
// what arguments it takes, and what it returns.
double distance(double *vect1, double *vect2, int coldim);

#endif 

